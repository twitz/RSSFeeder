<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_item_home"
            android:icon="@drawable/ic_logo"
            android:title="@string/menu.drawer.home"
            />
        <item
            android:id="@+id/drawer_item_saved"
            android:icon="@drawable/ic_bookmark"
            android:title="@string/menu.drawer.saved"
            />
        <item
            android:id="@+id/drawer_item_add"
            android:icon="@drawable/ic_add_box"
            android:title="@string/menu.drawer.add"
            />
        <item
            android:id="@+id/drawer_item_all"
            android:icon="@drawable/ic_all"
            android:title="@string/menu.drawer.all"
            />
    </group>
</menu>